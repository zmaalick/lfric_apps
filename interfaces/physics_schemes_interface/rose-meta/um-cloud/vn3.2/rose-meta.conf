#==============================================================================
# CLOUD:
#==============================================================================

[namelist:cloud]
compulsory=true
description=Cloud parametrization
ns=namelist/Science/UM Cloud
sort-key=Section-A05
title=Cloud

[namelist:cloud=bm_ez_opt]
compulsory=true
description=Bimodal cloud scheme entrainment zone options
!enumeration=true
help=In the bimodal cloud-scheme
    = (or bimodal-based initiation with PC2),
    = There are several options for how to represent the skewness of
    = the sub-grid saturation distribution at entrainment zones:
    = (1) - Original method; each model-level is diagnosed to be either
    =       in an entrainment zone or not (based on the presence of a
    =       local maximum in dTl/dz).  Model-levels inside an entrainment
    =       zone have skewness represented by a combination of modes drawn
    =       from discreet model-levels at the base and top of that zone.
    =       Model-levels not diagnosed to be in an entrainment zone
    =       have a symmetrical distribution / no skewness.
    = (2) - Subcritical-only method; as (1), but the diagnosis of an
    =       entrainment zone additionally requires that the zone overlaps
    =       with a turbulent layer (either Ri < 1, or below the top height
    =       of non-local boundary-layer mixing).
    = (3) - Entraining parcel method; all model-levels are allowed to have
    =       skewness, represented by drawing modes from above and below
    =       based on the properties of entraining parcels subsided from
    =       above / ascended from below.  The entrainment rate scales with
    =       1 / the turbulent length-scale Km/sqrt(w_var),
    =       so that the parcels tend to the current environment values if
    =       the turbulent length-scale is small
    =       (so that skewness becomes negigible in this limit).
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A10
trigger=namelist:cloud=ent_coef_bm: 'entpar';
value-titles=Original method,
            =Subcritical-only method,
            =Entraining parcel method
values='orig','subcrit','entpar'

[namelist:cloud=cff_spread_rate]
compulsory=true
description=Ice cloud fraction spreading rate
help=When the effects of shear on falling ice cloud are either ignored
    = or derived from model winds,
    =this controls the rate at which ice
    = cloud fraction spreads.
!kind=default
ns=namelist/Science/UM Cloud
range=0.0:1.0e-1
sort-key=Panel-A04a
type=real

[namelist:cloud=cld_fsd_hill]
compulsory=true
description=Whether to use the Hill scheme for fractional standard deviation of condensate
help=If true:  uses the Hill et al (2015) parametrization of fractional standard deviation
    =          (FSD) of cloud condensate in both the microphysics and the radiation schemes.
    =If false: uses the Boutle et al (2014) scheme for fractional standard deviation in the
    =          the microphysics and a time-and-space invariant value for radiation.
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A06
trigger=namelist:cloud=cloud_horizontal_liq_fsd: .false.;
       =namelist:cloud=cloud_horizontal_ice_fsd: .false.;
       =namelist:cloud=fsd_min_conv_frac: .true.;
       =namelist:cloud=fsd_conv_const: .true.;
       =namelist:cloud=fsd_nonconv_liq_const: .true.;
       =namelist:cloud=fsd_nonconv_ice_const: .true.;
type=logical

[namelist:cloud=cloud_call_b4_conv]
compulsory=true
description=Extra call to the large-scale-cloud scheme before convection
help=Switch to perform an extra call to the large-scale cloud scheme before the
    = call to convection in atmos_physics2, to ensure the latest T,q,qcl,cf
    = seen by convection are consistent with the cloud scheme assumptions.
    = For PC2, this entails an extra call to PC2 checks and PC2 initiation
    = before convection, and the application of cloud homogeneous forcing
    = tendencies from the transport scheme.
    = For diagnostic cloud schemes, the latest thermodynamic and cloud
    = variables are reset before convection by calling the cloud-scheme.
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A12
type=logical

[namelist:cloud=cloud_horizontal_ice_fsd]
compulsory=true
description=Relative standard deviation of ice cloud condensate
help=Defines the sub-grid ice cloud horizontal inhomogeneity using a single
    =global value of the ice cloud condensate relative standard deviation
    =(standard deviation / mean).
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A07
type=real

[namelist:cloud=cloud_horizontal_liq_fsd]
compulsory=true
description=Relative standard deviation of liquid cloud condensate
help=Defines the sub-grid liquid cloud horizontal inhomogeneity using a single
    =global value of the liquid cloud condensate relative standard deviation
    =(standard deviation / mean).
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A07
type=real

[namelist:cloud=cloud_pc2_tol]
compulsory=true
description=PC2 threshold liquid cloud fraction to retain cloud (RH < RHcrit)
help=When RH is below RHcrit, instances of liquid-cloud fraction below
    =cloud_pc2_tol are reset to zero at the end of each timestep.
    =Also instances of liquid-cloud fraction above 1 - cloud_pc2_tol
    =are reset to 1.
    =This threshold is also used to determine when to initiate cloud
    =from clear-sky (or holes in cloud from fully overcast)
    =under the "simplified" PC2 initiation logic option (pc2_init_logic=2).
    =
    =Note that pc2_checks performs an independent additional check to remove
    =liquid-cloud fractions below cloud_rounding_tol = 1.0E-12.
!kind=double
ns=namelist/Science/UM Cloud
range=1.0e-12:1.0e-1
sort-key=Panel-A02b
type=real

[namelist:cloud=cloud_pc2_tol_2]
compulsory=true
description=PC2 threshold liquid cloud fraction to retain cloud (RH > RHcrit)
fail-if=this > namelist:cloud=cloud_pc2_tol
help=When RH is above RHcrit, instances of liquid-cloud fraction below
    =cloud_pc2_tol_2 are reset to zero at the end of each timestep.
    =Also instances of liquid-cloud fraction above 1 - cloud_pc2_tol_2
    =are reset to 1.
    =This threshold is also used in a couple other places:
    = - Optical depth filtered cloud diagnostics are set to zero where
    =   the combined cloud fraction (stratiform + convective)
    =   is below the threshold.
    = - The riming process in the Wilson Ballard microphysics scheme is only
    =   performed where the mixed-phase cloud-fraction exceeds the threshold.
    =
    =Note that pc2_checks performs an independent additional check to remove
    =liquid-cloud fractions below cloud_rounding_tol = 1.0E-12.
    =
    =Also note that cloud_pc2_tol_2 should not exceed cloud_pc2_tol.
!kind=double
ns=namelist/Science/UM Cloud
range=1.0e-12:1.0e-1
sort-key=Panel-A02b
type=real

[namelist:cloud=dbsdtbs_turb_0]
compulsory=true
description=PC2 erosion rate parameter / s-1
help=Controls the rate of turbulent dissipation of liquid-cloud in grid-mean
    =subsaturated environments (and the rate of filling-in of sub-grid
    =clear-sky fraction in grid-mean supersaturated environments).
    =This process corresponds to narrowing the width of the sub-grid
    =moisture distribution.  The rate of turbulent dissipation of cloud liquid
    =water-content is parameterised as:
    =
    =  dqcl/dt = -K 2 Cl (1 - Cl) (qsat(T) - q)
    =
    =where q is the grid-mean vapour mixing-ratio, qsat(T) is the saturation
    =vapour mixing-ratio w.r.t. liquid water at the grid-mean temperature,
    =Cl is the liquid cloud-fraction, and K is the rate coefficient
    =given by this namelist input.
    =This formula makes the rate of cloud erosion scale with the
    =subsaturation of the environment, and an estimate of the
    =sub-grid area of exposed cloud edges.
    =The rate of change of Cl is then calculated based on the width-narrowing
    =rate which is consistent with the computed rate of evaporation of qcl.
!kind=double
ns=namelist/Science/UM Cloud
range=0.0:1.0e-2
sort-key=Panel-A09b
type=real

[namelist:cloud=ent_coef_bm]
compulsory=true
description=Bimodal cloud-scheme parcel method entrainment coefficient
help=In the bimodal cloud-scheme, when representing the skewness of the
    =sub-grid saturation distribution using entraining parcels
    =(bm_ez_opt='entpar'), this dimensionless coefficient scales the rate of
    =dilution of the modes from above and below towards the current
    =model-level values.
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A10
type=real

[namelist:cloud=ez_max]
compulsory=true
description=Maximum entrainment zone depth for bimodal cloud scheme (m)
help=The bimodal cloud scheme (or bimodal initiation in PC2) searches for
    = entrainment zones below sharp inversions. Ez_max sets a limit to how
    = deep entrainment zones can become. The recommended value is 400m.
!kind=default
ns=namelist/Science/UM Cloud
range=300.0:500.0
sort-key=Panel-A10
type=real

[namelist:cloud=falliceshear_method]
compulsory=true
description=Select how shear affects falling ice cloud fraction
!enumeration=true
help=Falling ice increases the ice cloud fraction in the layer it falls
    = into. There is the option to include the effects of the further
    = increase in the grid-box cloud fraction due to the lateral
    = displacement as the ice falls because of the vertical shear of
    = the horizontal wind. Either:
    = a) the effects of shear can be ignored,
    = b) a globally constant shear (not calculated from the actual wind)
    =    can be assumed or
    = c) the effects of shear are considered and the shear is calculated
    =    from the model wind.
    =
    =When not using the globally constant value an additional ice cloud
    = fraction source term is used to represent the lateral spreading out
    = of ice cloud fraction while maintaining the same grid-box mean
    = ice water content.
ns=namelist/Science/UM Cloud
sort-key=Panel-A04
trigger=namelist:cloud=cff_spread_rate: 'off','real'
value-titles=Ignore the effects of shear,
            =Assume a globally constant shear,
            =Use shear derived from model winds
values='off','constant','real'

[namelist:cloud=filter_optical_depth]
compulsory=true
description=Apply optical depth filter for cloud diagnostics.
help=If false: outputs the derived cloud diagnostics without any processing.
    =If true:  makes a simple estimate of the ice optical depth in each layer
    =          and ignores optically thin layers when calculating derived
    =          cloud diagnostics.
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A11
trigger=namelist:cloud=opt_depth_thresh: .true.
type=logical

[namelist:cloud=fsd_conv_const]
compulsory=true
description=Inhomogeneity factor for convective cloud
help=Under some options for the representation of subgrid scale horizontal
    = ice and liquid water content variability there is a
    = different parameter scaling the variability under a convective
    = cloud regime compared to a non-convective cloud one. Here the user
    = can set that parameter for the convective regime. Originally in
    = Hill et al (2015) this was set to 2.54 (their Eq 3) while GA7 to GA9
    = use the larger value of 2.81 (so more inhomogeneous)
!kind=default
ns=namelist/Science/UM Cloud
range=0.0:
sort-key=Panel-A07b
type=real

[namelist:cloud=fsd_min_conv_frac]
compulsory=true
description=Minimum convective fraction in FSD parametrization
help=The minimum convective cloud fraction under which to consider a
    = grid-box as convective when applying the Hill et al. (2015)
    = fractional standard deviation parametrization
!kind=default
ns=namelist/Science/UM Cloud
range=0.0:1.0
sort-key=Panel-A07a
type=real

[namelist:cloud=fsd_nonconv_ice_const]
compulsory=true
description=Inhomogeneity factor for non-convective ice cloud
help=Under some options for the representation of subgrid scale horizontal
    = ice and liquid water content variability there is a
    = different parameter scaling the variability under a convective
    = cloud regime compared to a non-convective cloud one. Here the user
    = can set that parameter for the non-convective regime. Originally in
    = Hill et al (2015) this was set to 0.96 (their Eq 3) while GA7 to GA9
    = use the larger value of 1.14 (so more inhomogeneous)
!kind=default
ns=namelist/Science/UM Cloud
range=0.0:
sort-key=Panel-A07d
type=real

[namelist:cloud=fsd_nonconv_liq_const]
compulsory=true
description=Inhomogeneity factor for non-convective liquid cloud
help=Under some options for the representation of subgrid scale horizontal
    = ice and liquid water content variability there is a
    = different parameter scaling the variability under a convective
    = cloud regime compared to a non-convective cloud one. Here the user
    = can set that parameter for the non-convective regime. Originally in
    = Hill et al (2015) this was set to 0.96 (their Eq 3) while GA7 to GA9
    = use the larger value of 1.14 (so more inhomogeneous)
!kind=default
ns=namelist/Science/UM Cloud
range=0.0:
sort-key=Panel-A07c
type=real

[namelist:cloud=ice_width]
compulsory=true
description=Width of vapour distribution in liquid-free part of gridbox
help=Ice width specifies the ice content (in terms of a fraction of
    = qsat_liq) that corresponds to a factor of two reduction in the
    = width of the vapour distribution in the liquid-free part of the
    = gridbox. This is only available if a subgrid partitioning of
    = out-of-liquid-cloud water vapor is requested (subgrid_qv=true).
!kind=default
ns=namelist/Science/UM Cloud
range=0.0:1.0
sort-key=Panel-A05a
type=real

[namelist:cloud=l_bm_sigma_s_grad]
compulsory=true
description=Account for local gradients in sub-grid saturation variance
help=In the bimodal cloud-scheme
    = (or bimodal-based initiation with PC2),
    = correct the saturation variance sigma_S given to each mode of the
    = sub-grid moisture PDF, to account for the local vertical gradient
    = in grid-mean saturation.  Also corrects the calculation of
    = dqsat/dz following a parcel to account for the change in pressure.
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A10
type=logical

[namelist:cloud=l_bm_tweaks]
compulsory=true
description=Make several miscellaneous minor tweaks to the bimodal cloud-scheme
help=In the bimodal cloud-scheme
    = (or bimodal-based initiation with PC2),
    = this switch activates the following minor changes:
    = a) Impose a minimum limit on the turbulent moisture variance,
    =    consistent with the limit already imposed when determining
    =    where condensation should occur.
    = b) Truncate the Gaussian distributions at max_sigmas standard deviations,
    =    consistent with the logic determining where condensation should occur.
    = c) Add extra checks to prevent the scheme from predicting negative qcl
    =    where q is negative.
    = d) Also avoid negative qcl resulting from floating point rounding error
    =    in a calculation of mixed-phase fraction.
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A10
type=logical

[namelist:cloud=l_ensure_max_in_cloud_pc2]
compulsory=true
description=Impose a max limit on in-cloud water contents (liquid and ice)
help=The max allowed in-cloud water content qc/cf is calculated interactively;
    =it scales with the mean total-water q+qcl+qcf in the column below
    =the current level.  It also scales with the cloud-fraction, so that
    =the limit is lower at points with very tiny cloud-amounts
    =(these points are the ones most likely to have ill-constrained
    = in-cloud water content, since the transport scheme applied
    = independently to condensate mass and cloud-fraction can create tiny
    = inconsistent values of the two near the edges of advected clouds).
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A09b
type=logical

[namelist:cloud=max_sigmas]
compulsory=true
description=Max width of bimodal cloud-scheme Gaussian PDFs
help=The bimodal cloud-scheme assumes the local turbulent moisture
    =fluctuations follow Gaussian PDFs.  Strictly speaking, Gaussians
    =never quite fall to zero, so technically assuming Gaussian dsitributions
    =can never yield zero cloud-amount.  To avoid getting silly tiny
    =cloud-fractions everywhere, the Gaussians are truncated at a certain
    =number of standard deviations from their means.  This namelist
    =input sets that number.
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A10
type=real

[namelist:cloud=min_sigx_ft]
compulsory=true
description=Bimodal cloud-scheme minimum free troposphere moisture variability
help=The bimodal cloud-scheme diagnoses the standard deviation of sub-grid
    =moisture variability based on boundary-layer scheme turbulence fields.
    =However, often the free troposphere can contain large moisture variability
    =in non-turbulent conditions (e.g. leftover from earlier convection).
    =This namelist input sets an imposed minimum standard deviation of the
    =unimodal moisture PDFs (in units of RH), applied only in the free
    =troposphere, to account for missed non-turbulent moisture variability.
    =Note that an equivalent "RHcrit" (threshold RH for cloud formation)
    =is given by 1 - min_sigx_ft * max_sigmas.
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A10
type=real

[namelist:cloud=mphys_erosion]
compulsory=true
description=Include PC2 erosion as a microphysical term
help=If true:  PC2 erosion is included with the microphysics code
    =If false: PC2 erosion is included with the convection code
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A09
type=logical

[namelist:cloud=opt_depth_thresh]
compulsory=true
description=Cloud diagnostics optical depth threshold.
help=Amount of ice optical depth below which cloud cover in
    = that layer is ignored when using filter_optical_depth.
!kind=default
ns=namelist/Science/UM Cloud
range=0.0:10.0
sort-key=Panel-A11a
type=real

[namelist:cloud=pc2_erosion_numerics]
compulsory=true
description=Numerical method for time integration of PC2 cloud erosion
!enumeration=true
help=The PC2 erosion term is often "fast" relative to the timestep length,
    =which causes problems with numerical overshoot; e.g. when convection
    =detrains cloud into a dry environment, the erosion rate is often
    =high enough to remove all the detrained cloud within the timestep.
    =
    =With the explicit numerical method,
    =this is addressed by not allowing erosion to act on
    =the current timestep convective cloud increment, so that some
    =detrained cloud survives to the end of the timestep.
    =However, that method yields an end-of-timestep cloud amount which
    =scales with the timestep length, which is problematic.
    =
    =With the implicit numerical method, erosion is allowed
    =to act on the fully updated cloud fields after convection,
    =but is solved such that the erosion rate applied is approximately
    =consistent with the cloud-fraction that will occur after the
    =erosion increment has been added on.
    =This avoids the numerical overshoot, while reducing timestep sensitivity.
    =
    =One problem with the implicit method is that it makes it impossible for
    =erosion to completely remove cloud.  This makes it easy for numerical
    =diffusion in the model to spread small cloud amounts far and wide,
    =even into grid-points with low RH where there shouldnt be any cloud.
    =
    =A 3rd option computes an approximate analytical solution to the equations
    =posed for the erosion, such that the rate of removal of qcl does decline
    =realistically as cfl approaches zero, but the cloud can eventually
    =be eroded away completely.
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A09b
value-titles=Explicit forwards-in-time,
            =Implicit w.r.t. cloud-fraction,
            =Analytical solution
values='explicit','implicit','analytic'

[namelist:cloud=pc2_homog_g_method]
compulsory=true
description=Method to use for calculating the amplitude of the
           =moisture PDF in PC2 homogeneous forcing.
!enumeration=true
help=Under the assumptions made for calculating homogeneous forcing
    =of liquid cloud in PC2, the known constraints
    =(cloud-fraction, liquid-water content, saturation defecit, and
    =the power of the assumed PDF) provide 2 different solutions
    =for the magnitude of the moisture PDF at the saturation boundary g(-qc),
    =which scales the cloud fraction increments:
    =(a) one based on cloud-fraction / liquid water content,
    =(b) the other based on (1 - cloud-fraction) / saturation defecit.
    =There are options for how to parameterise g(-qc) as a
    =function of these:
    =
    =(cf) Use a blend of (a) and (b) weighted by a function of cloud-fraction.
    =Note that (a) can become extremely large following any process
    =which removes liquid water without reducing the cloud-fraction
    =(e.g. rain-out by the microphysics scheme).
    =Even a weakly positive homogeneous forcing in this situation can
    =result in unfeasibly large positive cloud-fraction increments
    =(e.g. CF can suddenly jump up to 1.0 in a subsaturated grid-cell).
    =Another issue is that the code for this method has an IF test for
    =safety which results in homgeneous forcing spuriously yielding
    =zero cloud increments if either qcl or sd is near-zero.
    =
    =(width) Use a blend of (a) and (b) weighted by the PDF-widths
    =consistent with each.  This avoids the problems with (1) described above,
    =since the contribution from (a) will go to zero when there is
    =no liquid water content.
    =
    =(rev) Use finite integrals over the PDF instead of explicit numerics,
    =with revised blending between the two solutions to make the homogeneous
    =forcings accurately reversible.  This is intended to more reliably give
    =sensible answers when using long timesteps / the forcing increments
    =are too large to get away with only using G(-Qc) at the instantaneous
    =saturation boundary.  It also avoids spurious drift in the cloud-fraction
    =when equal and opposite forcings occur from different processes.
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A09b
value-titles=Blend as a function of cloud-fraction,
            =Blend as a function of PDF-width,
            =Blend using accurately reversible finite integral method
values='cf','width','rev'

[namelist:cloud=pc2_init_logic]
compulsory=true
description=Options for where to allow PC2 initiation to operate
!enumeration=true
help=(original) Original logic;
    = Tests are performed to check whether the cloud cover is 0 or 1
    = (but if it is below zero degree C we check for cloud cover less
    = than 0.05 or greater than 1).
    = Tests on being above the LCL when considering initiating away from 0 are
    = also done early on, confusing the logic somewhat.
    = Also the logic prevents PC2 initiating cloud above a convective
    = boundary layer (because of assumption that there is a convection
    = scheme in use that will detrain cloud and create cloud that way instead).
    = This is an issue in a convection-permitting model where there is no
    = convection scheme to create that cloud via detrainment.
    =
    =(smooth) Much simpler logic that should yield smoother behaviour;
    = The diagnostic cloud scheme calculations (Smith or Bimodal)
    = are performed at any grid-point where they are expected to yield
    = nonzero liquid water content, regardless of the existing cloud fraction.
    = The diagnosed qcl is then taken as a minimum limit applied
    = to the PC2 prognostic qcl.  Cloud fraction is adjusted towards the
    = diagnostic scheme value in proportion to the fractional increase
    = of qcl by initiation (or the fractional increase of saturation deficit
    = at points which are grid-mean total-water supersaturated).
    =
    =(smooth_fix) As (smooth), but the cloud-fraction is hard-limited by
    = the diagnostic scheme value instead of being adjusted only partially
    = towards it, consistent with the treatment of qcl
    = (diagnostic cf_liq is taken as a minimum limit when grid-mean total-water
    =  sub-saturated, but as a max limit when super-saturated).
    = This avoids a numerical problem with option (3), where qcl is hard
    = limited while cf_liq is allowed to be reduced below its diagnosed
    = value by PC2 erosion, resulting in the in-cloud water content
    = qcl/cf_liq drifting upwards where erosion occurs
    = (which will promote spurious rain production in the microphysics).
ns=namelist/Science/UM Cloud
sort-key=Panel-A02b
value-titles=Original,Smooth,Smooth fixed
values='original','smooth','smooth_fix'

[namelist:cloud=pc2_init_method]
compulsory=true
description=Method for doing PC2 initiation
!enumeration=true
help=Smith-like initiation as described in Wilson et al (2008). When cloud
    = fraction is 0 or 1, cloud will be generated or removed based on a
    = triangular subgrid PDF with a width based on the RHcrit profile.
    =
    =Bimodal scheme: call the bimodal cloud scheme when cloud fraction is 0
    = or 1 to determine whether cloud should be initiated or removed based
    = on a bimodal Gaussian subgrid PDF with variances determined from the
    = turbulent properties as in Furtado et al. (2016).
ns=namelist/Science/UM Cloud
sort-key=Panel-A02a
value-titles=Smith,Bimodal
values='smith','bimodal'

[namelist:cloud=pc2_turb_horiz]
compulsory=true
description=Do PC2 homogeneous forcing from horizontal turbulent mixing
help=If this is false, the vertical component of turbulent mixing
    =(the 1D boundary-layer scheme) is treated as a homogeneous forcing
    =process to compute corresponding increments to liquid cloud fraction
    =and water content, but the horizontal component of turbulent mixing
    =(active when using the 3D Smagorinsky or blended turbulence scheme)
    =is neglected.  Horizontal turbulent mixing is applied to vapour q,
    =liquid water content qcl and theta, but liquid-cloud fraction is unaltered
    =and so may become inconsistent with the other variables.
    =
    =If this is true, the horizontal turbulent mixing increments to
    =q+qcl and Tl are computed and increments to qcl and cloud-fraction
    =are calculated using the homogeneous forcing approach, consistent
    =with the vertical mixing.
ns=namelist/Science/UM Cloud
sort-key=Panel-A02c
type=logical

[namelist:cloud=rh_crit]
!bounds=namelist:extrusion=number_of_layers
compulsory=true
description=Critical relative humidity
fail-if=len(this) != namelist:extrusion=number_of_layers ;
help=The critical relative humidity is used in the cloud scheme to define
    = the ratio at which cloud starts to form. Values may vary depending on
    = the configuration. Please refer to standard jobs for current
    = recommended settings.
    =
    = The number of values must match the number of levels in the model.
    =
!kind=default
length=:
ns=namelist/Science/UM Cloud
range=0.0:1.0
sort-key=Panel-A03
type=real

[namelist:cloud=rh_crit_opt]
compulsory=true
description=RHCrit parametrization method
!enumeration=true
help=If no parametrization is selected,
    =the values in the table below
    = are used.
    =
    =The boundary layer TKE based method predicts the sub-grid
    = variability based on the gradients,
    =eddy diffusivities and
    = TKE predicted by the boundary layer scheme. It does still
    = make use of the table values in the microphysics code for
    = parametrizing the RH of the clear sky portion of the grid-box.
ns=namelist/Science/UM Cloud
sort-key=Panel-A02
value-titles=No parametrization,
            =Boundary layer TKE based method
values='namelist','tke'

[namelist:cloud=scheme]
compulsory=true
description=Cloud scheme
!enumeration=true
help=Choice of UM cloud scheme to use
    =
    = Smith scheme (1990):
    = Uses the diagnostic cloud fraction and condensate variables,
    = based on a symmetric triangualar PDF of subgrid variability.
    =
    = Bimodal scheme (Van Weverberg et al 2021)
    = Uses the diagnostic cloud fraction and condensate variables,
    = based on a bimodal PDF of sugrid variability.
    =
    = PC2 scheme (Wilson et al 2008):
    = Uses the prognostic cloud and prognostic condensate scheme.
    =
    = Note: Disabling the cloud scheme will result in no updates to
    =       the cloud fraction or water content from the cloud scheme.
    =       Micro-physics updates to cloud are unaffected by this
    =       setting.
    =
ns=namelist/Science/UM Cloud
sort-key=Panel-A01
trigger=namelist:cloud=mphys_erosion: 'pc2' ;
       =namelist:cloud=dbsdtbs_turb_0: 'pc2' ;
       =namelist:cloud=ez_max: 'pc2', 'bimodal' ;
       =namelist:cloud=bm_ez_opt: 'pc2', 'bimodal' ;
       =namelist:cloud=l_bm_sigma_s_grad: 'pc2', 'bimodal' ;
       =namelist:cloud=l_bm_tweaks: 'pc2', 'bimodal' ;
       =namelist:cloud=l_ensure_max_in_cloud_pc2: 'pc2' ;
       =namelist:cloud=max_sigmas: 'pc2', 'bimodal' ;
       =namelist:cloud=min_sigx_ft: 'pc2', 'bimodal' ;
       =namelist:cloud=pc2_erosion_numerics: 'pc2' ;
       =namelist:cloud=pc2_homog_g_method: 'pc2' ;
       =namelist:cloud=pc2_init_logic: 'pc2' ;
       =namelist:cloud=pc2_init_method: 'pc2' ;
       =namelist:cloud=turb_var_fac_bm: 'pc2', 'bimodal' ;
       =namelist:cloud=pc2_turb_horiz: 'pc2';
       =namelist:external_forcing=pc2_force_response: 'pc2';
value-titles=Smith, PC2, Bimodal
values='smith', 'pc2', 'bimodal'

[namelist:cloud=subgrid_qv]
compulsory=true
description=Calculate a partitioning of water vapor outside of liquid cloud
help=Setting this option to true partitions the out-of-liquid-cloud water vapor
    =in each gridbox between clear-sky and sublimating and depositing ice-cloud
    =regions. If this is turned off then qv is the same throughout the
    =clear-sky and ice-cloud parts of each gridbox. Which means that the ice
    =is either entirely sublimating of entirely growing,
    =depending on the value of
    =the in-ice-cloud relative humidity.
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A05
trigger=namelist:cloud=ice_width: .true.
type=logical

[namelist:cloud=turb_var_fac_bm]
compulsory=true
description=Bimodal cloud-scheme unimodal variance coefficient
help=In the bimodal cloud-scheme, this dimensionless coefficient
    =scales the widths of the local turbulent sub-grid saturation PDFs.
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A10
type=real

[namelist:cloud=two_d_fsd_factor]
compulsory=true
description=Factor to convert 1D FSD for straight line to 2D value
help=Current parametrizations of FSD (fractional standard deviation,
    =a measure
    =of subgrid variability) of cloud water content used in radiation and
    =microphysics schemes are based on observations taken in a 1D line. For the
    =UM we require the FSD for a 2D gridbox,
    =which is generally larger. We
    =account for this difference by multiplying the 1D fsd by this value.
!kind=default
range=1:2
sort-key=Panel-A08b
type=real

[namelist:cloud=use_fsd_eff_res]
compulsory=true
description=Whether to use a hardwired effective resolution in fractional standard deviation (FSD) scheme
help=If true:  use the FSD equations (from Boutle et al (2014) or Hill et al (2015))
    =          with a fixed grid-size representative of N96/C48.
    =If false: use the FSD equations (from Boutle et al (2014) or Hill et al (2015))
    =          with an estimate of the grid-size calculated from the grid.
!kind=default
ns=namelist/Science/UM Cloud
sort-key=Panel-A08a
type=logical
